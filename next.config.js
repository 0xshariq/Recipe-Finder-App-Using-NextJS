/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['www.themealdb.com'], // Configure external image domains
    },
  };
  
  module.exports = nextConfig;
  